const config = {
    foo: 'bar',
    players: ['North','East','South','West'],
    player_zones: ['hand','keep'],
    common_zones: ['play','deck','pile'],
    phases: ['draw','play'],
} //as const

export default config